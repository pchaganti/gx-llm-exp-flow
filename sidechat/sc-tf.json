[
  {
    "type": "function",
    "function": {
      "name": "run_command",
      "description": "Run command, this is what you should try first",
      "parameters": {
        "type": "object",
        "properties": {
          "cmd": {
            "type": "string",
            "description": "The command you wish to run"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "list_files",
      "description": "List files in a given path",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "An optional path to search for files, otherwise cwd"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "run_bash_command",
      "description": "run a command in bash - only do this if run_cmd fails",
      "parameters": {
        "type": "object",
        "properties": {
          "cmd": {
            "type": "string",
            "description": "The command line to run"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "create_file",
      "description": "Create a file at a given path",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "A relative path to create a file"
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "edit_file",
      "description": "Edit a file by replacing content between specific lines or replacing specific text",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "The path to the file to edit"
          },
          "line_start": {
            "type": "integer",
            "description": "The starting line number to replace (inclusive)"
          },
          "line_end": {
            "type": "integer",
            "description": "The ending line number to replace (inclusive)"
          },
          "old_content": {
            "type": "string",
            "description": "The specific text to replace"
          },
          "new_content": {
            "type": "string",
            "description": "The new content to insert"
          }
        },
        "required": ["path", "line_start", "line_end", "old_content", "new_content"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "read_file",
      "description": "Read a file and return content with XML line numbering. Output format: <line number=N>{line content}</line> for each line. Use line numbers from output to make precise edits.", 
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "The path of the flie"
          },
          "line_start": {
            "type": "integer",
            "description": "Optional starting line number to read from (1-indexed)"
          },
          "line_end": {
            "type": "integer",
            "description": "Optional ending line number to read to (inclusive)"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "save_memory",
      "description": "Save a memory. It should be in the form 'The User' or if you have the name, use the name.",
      "parameters": {
        "type": "object",
        "properties": {
          "memory": {
            "type": "string",
            "description": "The memory to save"
          }
        },
        "required": ["memory"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "read_pydoc",
      "description": "Show the pydoc for some object",
      "parameters": {
        "type": "object",
        "properties": {
            "object": {
                "type": "object",
                "description": "The object to get the documentation on"
            }
        },
        "required": ["object"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "read_man_section",
      "description": "Show a contextual snippet of a man page",
      "parameters": {
        "type": "object",
        "properties": {
            "manpage": {
                "type": "string",
                "description": "The manpage to view"
            },
            "parameter": {
                "type": "string",
                "description": "The parameter or command to view, such as '-z' or 'compgen'"
            }
        },
        "required": ["manpage", "parameter"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "show_memory",
      "description": "Show all memories - check here if you do not know something",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
]
